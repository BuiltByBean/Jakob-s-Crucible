# Pinned to the EXACT versions this was built and tested against, so a
# breaking major can't land unreviewed on the next platform build and a code
# revert rolls everything back. Bump versions deliberately, as their own
# reviewed commits.
flask==3.1.3
flask-sqlalchemy==3.1.1
flask-wtf==1.2.2
flask-mail==0.10.0
gunicorn==26.1.0
# Reads the owner's Word manuscripts when he uploads one in /admin — a
# production dependency since that conversion now happens in a request.
python-docx==1.2.0
# lxml is python-docx's compiled transitive: pinned per this file's policy, since a
# release without a cp312 manylinux wheel would break the image build with no code change.
lxml==6.1.2
# Load-bearing transitives — pinned explicitly so they can't float either.
sqlalchemy==2.0.48
werkzeug==3.1.7
jinja2==3.1.6
wtforms==3.2.1
markupsafe==3.0.3
itsdangerous==2.2.0
blinker==1.9.0
click==8.3.1
